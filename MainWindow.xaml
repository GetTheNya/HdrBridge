<Window x:Class="SyncLightBridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:SyncLightBridge.Converters"
        Title="SyncLight Bridge" Height="580" Width="480"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        FontFamily="Segoe UI">

    <Window.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Panel" Padding="20,10" Margin="0,0,4,0" Background="#2D2D30" CornerRadius="4,4,0,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Panel" Property="Background" Value="#007ACC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding CurrentModeString}" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <TabControl Grid.Row="1" Background="#252526" BorderThickness="0" Foreground="White">
            <!-- HYPERHDR TAB -->
            <TabItem Header="HyperHDR Sync">
                <StackPanel Margin="20">
                    <TextBlock Text="This mode bridges HyperHDR UDP frames directly to your LED strip. Make sure HyperHDR is broadcasting to the correct UDP port." TextWrapping="Wrap" Margin="0,0,0,25" />
                    
                    <Border Background="#1E1E1E" CornerRadius="8" Padding="15" Margin="0,0,0,25">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="{Binding IsUsbConnected, Converter={StaticResource BoolToColorConverter}}" Margin="0,0,8,0"/>
                                <TextBlock Text="USB Connected" FontWeight="SemiBold"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="{Binding IsUdpListening, Converter={StaticResource BoolToColorConverter}}" Margin="0,0,8,0"/>
                                <TextBlock Text="UDP Listening" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Button Content="Activate HyperHDR Sync" Command="{Binding ActivateHyperHDRCommand}" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="12" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </TabItem>

            <!-- HARDWARE EFFECTS TAB -->
            <TabItem Header="Hardware Effects">
                <StackPanel Margin="20">
                    <TextBlock Text="Trigger built-in hardware effects standalone. This will bypass the HyperHDR stream and use the LED strip's internal effects structure." TextWrapping="Wrap" Margin="0,0,0,25"/>

                    <TextBlock Text="Select Effect" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding AvailableEffects}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedHardwareEffect}" Margin="0,0,0,30" Padding="6" Background="#2D2D30" Foreground="White"/>

                    <Button Content="Activate Hardware Effect" Command="{Binding ActivateHardwareModeCommand}" Background="#9C27B0" Foreground="White" BorderThickness="0" Padding="12" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </TabItem>

            <!-- SETTINGS TAB -->
            <TabItem Header="Settings">
                <StackPanel Margin="20">
                    <TextBlock Text="UDP Port (Default 19446)" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SettingsService.CurrentSettings.UdpPort, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="LED Count" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SettingsService.CurrentSettings.LedCount, UpdateSourceTrigger=PropertyChanged}"/>

                    <CheckBox Content="Start Minimized with Windows" IsChecked="{Binding SettingsService.CurrentSettings.StartWithWindows}" Foreground="White" Margin="0,15,0,30"/>

                    <Button Content="Apply &amp; Save Settings" Command="{Binding SaveSettingsCommand}" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="12" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
